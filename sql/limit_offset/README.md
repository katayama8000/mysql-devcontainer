# LIMIT and OFFSET Examples

This directory contains SQL examples demonstrating the use of `LIMIT` and `OFFSET` clauses for pagination and limiting result sets in MySQL.

- `limit_offset_example.sql`: Provides examples of selecting a limited number of rows and offsetting the starting point to retrieve subsequent sets of rows.
